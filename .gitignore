# ---- Dependencias y builds ----
node_modules/
.next/
out/
dist/
build/
target/
*.class
*.tsbuildinfo

# ---- Variables de entorno / secretos ----
.env
.env.local
.env.*.local
!.env.example
backend/.env
frontend/.env.local
*.pem
*.key

# ---- Herramientas locales (JDK, Maven, etc.) ----
.tools/

# ---- IDE / SO ----
.idea/
.vscode/
*.iml
.DS_Store
Thumbs.db
desktop.ini

# ---- Logs ----
*.log
hs_err_pid*

# ---- Resquício antigo — frontend agora está em repo separado ----
# Obs.: os arquivos de `frontend/` que já estavam versionados continuam RASTREADOS pelo Git
# (o .gitignore não se aplica a arquivos já rastreados). Remover do índice exigiria
# `git rm -r --cached frontend` — decisão pendente (A VALIDAR).
frontend/